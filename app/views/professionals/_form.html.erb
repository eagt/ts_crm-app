

<%= error_messages_for(@professional) %> 

		<table summary = " Professional form fields " >	
			<!-- This template will work only if I give or set a value to the variable f in the new and edit templates-->
			
				 <tr>
	   			  <th><%= f.label(:id_code, t(:id)) %></th>
	   			  <td><%= f.text_field(:id_code) %></td>
	 			 </tr>
				 <tr>
	    		  <th><%= f.label(:first_name, t(:first_name)) %></th>
	    		  <td><%= f.text_field(:first_name) %></td>
	 			 </tr>			 
 				 <tr>
    			  <th><%= f.label(:last_name, t(:last_name)) %></th>
    			  <td><%= f.text_field(:last_name) %></td>
 				 </tr>
				 <tr>
				   <th><%= f.label(:dob, t(:dob)) %></th>
				   <td><%= f.date_select(:dob, {:start_year => 1900, :end_year => Time.now.year}) %></td>
				 </tr>
				 <tr>
				   <th><%= f.label(:email, t(:email)) %></th>
				   <td><%= f.text_field(:email) %></td>
				 </tr>
				 <tr>
				   <th><%= f.label(:specialty, t(:specialty)) %></th>
				   <td><%= f.text_field(:specialty) %></td>
				 </tr>

			 
		<!-- <tr>
			 	<th><%= f.label(:pass_active, t(:pass_active)) %></th>
				<td><%= f.text_field(:pass_active) %></td>
			 </tr>
			 <tr>
				<th><%= f.label(:acc_active, t(:acc_active)) %></th>
				<td><%= f.text_field(:acc_active) %></td>
			 </tr>
			 <tr>
				<th><%= f.label(:password_digest, t(:password_digest)) %></th>
				<td><%= f.text_field(:password_digest) %></td>
			 </tr>
			 <tr>
				<th> Last in </th>
				<td><%= f.date_select(:dob, {:start_year => 1900, :end_year => Time.now.year}) %></td> This will be done authhomatically with touch, it probably will need to be set at the controller
			 </tr> -->



		</table>